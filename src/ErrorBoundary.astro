---
let html

// attempt to render children
try {
    html = await Astro.slots.render("default")
} catch (err) {
    html = await Astro.slots.render("error", [err])
}
---
<astro-error-boundary set:html={html} />